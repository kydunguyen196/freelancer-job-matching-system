server:
  port: 8085
  shutdown: graceful

spring:
  application:
    name: contract-service
  lifecycle:
    timeout-per-shutdown-phase: 20s

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/contract_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

  jpa:
    hibernate:
      ddl-auto: ${APP_JPA_DDL_AUTO:update}
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: ${APP_FLYWAY_ENABLED:true}
    baseline-on-migrate: ${APP_FLYWAY_BASELINE_ON_MIGRATE:true}

logging:
  pattern:
    level: "%5p [cid:%X{correlationId:-}]"
  level:
    org.hibernate.SQL: ${APP_SQL_LOG_LEVEL:debug}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-this-secret-key-to-at-least-32-characters-long-for-dev}
    access-token-expiration-ms: ${APP_JWT_ACCESS_TOKEN_EXPIRATION_MS:900000}
    refresh-token-expiration-ms: ${APP_JWT_REFRESH_TOKEN_EXPIRATION_MS:604800000}
  internal:
    api-key: ${APP_INTERNAL_API_KEY:change-this-contract-internal-api-key}
  outbox:
    enabled: ${APP_OUTBOX_ENABLED:true}
    batch-size: ${APP_OUTBOX_BATCH_SIZE:50}
    dispatch-interval-ms: ${APP_OUTBOX_DISPATCH_INTERVAL_MS:5000}
    initial-retry-delay-seconds: ${APP_OUTBOX_INITIAL_RETRY_DELAY_SECONDS:5}
    retry-multiplier: ${APP_OUTBOX_RETRY_MULTIPLIER:2.0}
    max-retry-delay-seconds: ${APP_OUTBOX_MAX_RETRY_DELAY_SECONDS:300}

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: ${APP_DOCS_ENABLED:true}
  swagger-ui:
    path: /swagger-ui.html
    enabled: ${APP_DOCS_ENABLED:true}
